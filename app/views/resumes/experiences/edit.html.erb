<%= render "layouts/nav" %>
<div class="max-w-sm mx-auto p-2">
  <p class="text-center mb-4">Edit Experiences</p>
  <% if @experience.experience_line_items.empty? && @experience != Experience.first %>
    <p class="text-sm">Would you like to copy items from the previous version?</p>
    <br>
    <%= link_to "Copy experience items", edit_resume_experience_path(@resume.id, @resume.experience.id, duplicate: true), class: "bg-blue-400 text-white rounded text-center p-2" %>
    <br>
    <br>
    <hr>
  <% end %>
  <%= render partial: "form" %>
</div>
